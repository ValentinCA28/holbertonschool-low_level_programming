#include "main.h"
#include <limits.h>

int _atoi(char *s)
{
	int i = 0;
	int x = 0;

	if (s[i] >= 0)
	{
		for (i = 0; s[i] != '\0'; i++)
			if(s[i] == ' ')
			{
				i += 1;
			}
			if(s[i] = "-")
			{
				s[i] = s[-i]*
				i++;
			}
			else
			{
				i++;
			}

	}
}
