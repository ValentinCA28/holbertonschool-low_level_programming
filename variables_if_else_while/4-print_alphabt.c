#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
	* main - Entry point
	*
	* Description: print reverse de 0 a 9 - rev py
	*
	* Return: Always 0 (Success)
	*/
int main(void)
{
int c;

for (c = 'a' ; c <= 'z' ; c++)
if (c != 'e' && c != 'q')
{
putchar(c);
}
{
putchar('\n');
}
return (0);
}
