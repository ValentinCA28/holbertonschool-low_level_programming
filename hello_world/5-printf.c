#include <stdio.h>
/**
* main - Entry point
*
* Description: 'print a string using puts function'
* Return: Always 0 (Success)
*/

int main(void)
{
	/* Afficher la chaîne de caractères avec printf */
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
